# 读数、出处与诚实的限制

## 这一页最重要的一句话

**本包没有任何一个关于这块列的收益读数。** `intraday_stats` 是数据层为本轮新建的派生表，本仓库从没有一条臂读过分钟派生的价格分布统计——这是它值一条臂的理由，也是它没有先验读数的原因。本包有的是：文献证据（`references/intraday-moments.md`）、载体自己的账单、起步包的构造检查，以及在本轮种子上实测的覆盖度、增益份额与实现成本（下文）。**第 0.5 轮的 G-INC2 是第一个收益读数。**

本页引用的仓库读数都测在研究期 2021-07-01..2025-06-30 之内，没有任何研究期之后的行。

## 「从来没有人读过它」是怎么量出来的

研究教训登记 §5 第 7 条的普查口径：当前纪元 46 条臂里，**没有一条打开过 `include_intraday`**，分钟派生的日频特征**从没进过任何排序器**。唯一读过分钟构造的是已关闭的 `order_flow_ranker_20260917`，它读的是带符号成交量（分钟 OFI），不是价格路径的分布矩。分钟域的代价登记在 §6：按成分过滤加向量化读一天，沪深 300 是 0.106 秒、常驻 11.4 MB；**受制的是内存不是时间**，所以数据层把它按股票日预聚合成一张事件表，策略侧只读这张表，不碰分钟行。

## 起步包实测

**构造检查（不需要快照数据，本包写成时做过）。**

- 起步包三个候选常量下都通过 `validate_strategy_package`（导入白名单与 AST 检查），全部模块 `py_compile` 通过。
- 块构建器的合成数据检查：手工造三只股票 30 个交易日的 `intraday_stats` 行，写成真实的 parquet 分片目录，经起步包自己的事件读取（`dataset` 与 `ts_code` 下推、`available_at` 过滤）进 `istats.build`，再用逐日循环的独立实现重算全部 15 列：1,350 个格子逐位一致；`n_bars` 掩码与 `rv = 0` 掩码生效；RSJ = 1 − 2 × `rv_down_share` 的恒等式成立；同一个域里另一个数据集的同键行不会混进来；决策之后才盖章的行不可见；快照里没有 `intraday_stats` 时直接报错。故意去掉 `dataset` 过滤后，这项检查按预期失败。
- 端到端再走一遍数据层自己的生产函数：合成三只股票 30 个交易日、每天 241 根的分钟 Bar（带隔夜跳空），交给数据层的 `intraday_stats` 聚合函数产出行，再经起步包读进块里；`rsj_1` 与直接从分钟收益独立算出的 RSJ 逐位一致（隔夜跳空按定义不计入），最后一根 bar 上 15 列全部有限。

**真实宿主流水线（实测）。** 同一个起步包走正式回放路径：控制台的创建前检查（`_round.normalize`，本轮 GATES 与回撤，`events_datasets` 按 `[…17 个…, intraday_flow, intraday_stats]` 的顺序，种子 `data/pit_views_seed_research_20260924`）→ `resolve_worker_options`（种子契约逐字段比对通过）→ `worker._strategy_sandbox_from_spec`（断言 `gpu_count=0`、`memory="16g"`、`cpus=8`）→ `ResearchPITSnapshotProvider` 从种子硬链接视图 → `PITDailyEvaluationBackend.evaluate`（`benchmark_index=000300.SH`）在沙箱镜像里回放，滚动 as-of 视图、真实 `AccountSnapshot`、Broker 与成本。100 万元，第四研究年，**70 个决策日（2024-07-01..2024-10-15），跨过 2024-10 的季度重训**。

| 腿 | 视图准备 | 墙钟 | `phase_seconds`：`data_view` / `fit` / `strategy` / `timeview_init` | `fit_seconds`（冷 / 续训） | 容器内存峰值 | 订单（成交 / 拒） | 换手 | 平均总仓位 |
|---|---|---|---|---|---|---|---|---|
| `c_base` | 0.0 秒（硬链接） | 343.6 秒 | 229.2 / 57.2 / 24.3 / 24.3 | 19.0 / 31.5（上限 3,600） | **2.36 GiB / 16** | 98（96 / 2） | 1.947 | 0.971 |
| `s1` | 0.0 秒（硬链接） | 435.5 秒 | 309.5 / 66.6 / 26.5 / 23.8 | **20.2 / 39.4** | **3.36 GiB / 16** | 98（97 / 1） | 1.973 | 0.971 |

- **没有一次 OOM、超时或 loader 拒绝。** `fit` 用掉上限的 0.6–1.1 %，内存峰值用掉 16 GiB 的 15–21 %；加 15 列让续训 `fit` 慢约四分之一、内存多约 1 GiB。两条腿 `data_view` 的差（229 对 309 秒）发生在同一份快照配置上，是宿主负载，不是本块。
- **续训与月度复核都走通了。** 两条腿的订单元数据里 `refits` 都在 2024-10-08 从 1 变 2，回放末的 `refit_meta` 读「2 次重训、最后一次是续训、验证 L2 0.082」；续训比冷启动慢（31.5 / 39.4 对 19.0 / 20.2 秒）。4 个买入日 / 3 个卖出日（07-01、08-01、09-02、10-08），成分截面逐月滚动四张。70 天换手扣掉首日建仓的 0.97 之后约 1.0，年化约 3.5 倍（双边）——粗读，P3 的实测仍要做。
- **账户面**：书恒为 50 席，可打分池 281–283 只，买不起的成分 2.1–3.2 %，座位 19,400–22,560 元，实际权重 0.011–0.024（目标 0.02），买单全是整手（200–7,000 股）；拒单都是 `missing_execution_price`（当天停牌），1–2 / 98。书覆盖的指数权重 `s1` 16.0–17.5 %、`c_base` 17.5–20.3 %。
- **块的读数**：`block_gain_share` 在冷 `fit` 上 **0.249**、续训后 **0.246**（`c_base` 恒 0，一致性检查通过），是按列数均分 8.7 % 的近三倍；`block_coverage` 1.0000 / 0.9985 / 0.9988 / 0.9903。
- **但这份增益不在文献那几列上。** 回放末 `s1` 的 booster（40 棵树）逐列增益占全部列：`vo30_l` **9.0 %**（全部 173 列里第 2）、`rku_l` **7.3 %**（第 3）、`rc30_l` 2.9 %、`rmid_l` 1.7 %、`vc30_l` 1.3 %、`ro30_s` 与 `rc30_s` 各 0.8 %、`vwd_s` 0.6 %、`rsj_l` 0.2 %，`rsj_1`、`rsj_s`、`sj_rel`、`rsk_s`、`rsk_l`、`vc30_z` 为 0。**增益份额过 5 % 的门，靠的是开盘成交量集中度与一个大半是价格水平的峰度列，不是 RSJ 与偏度**——这正是 `references/block-ablation.md` 说的「份额高不代表有增量」。这只是一次 `fit` 的读数，第 0.5 轮要在 60 个决策日上逐列重报，并在结果笔记里把它与符号一致性表并排写。
- **这 70 天的收益读数不可用于裁决**：`max_days` 路径不抽零技能面板，没有任何主动统计量。方向性的实现检查读数（不是成绩）：`s1` β 0.855、跟踪误差 5.6 %、`top_industry_weight` 0.198、`size_tilt` 0.945；`c_base` β 0.815、6.8 %、0.228、0.946。

**覆盖度普查（本轮种子的真实决策视图，离线）。** 行级读数取 2025-06-30 那张决策视图，按每个交易日在册的沪深 300 截面逐日数；「15 列全部有限」走起步包自己的路径——在 2023-06-30 与 2025-06-30 两张决策视图上，按 `fit` 的方式建面板并跑 `istats.build`，对每根可用 bar 取成分里 15 列全部有限的占比（第一研究年取前一张，第三、四研究年取后一张，第二研究年两张一致）。

| 研究年 | Y1 | Y2 | Y3 | Y4 |
|---|---|---|---|---|
| 有日线的成分股票日里有 `intraday_stats` 行 | 100 % | 100 % | 100 % | 100 % |
| 其中 11 个读入列全部非空 | 99.97 % | 99.98 % | 99.99 % | 99.97 % |
| `n_bars` = 241 的行 | 100 % | 100 % | 100 % | 100 % |
| 被掩码掩掉的行（全部是 `rv` = 0） | 25（0.034 %） | 15（0.021 %） | 4（0.006 %） | 24（0.033 %） |
| 15 列全部有限（可用 bar 均值 / 最低） | 99.86 % / 99.00 % | 99.92 % / 99.33 % | 99.99 % / 99.67 % | 99.68 % / 97.67 % |
| `rku_l` 与对数收盘价的截面秩相关（月末中位） | 0.589 | 0.585 | 0.640 | 0.597 |
| `rsk_l` 同上 | −0.004 | 0.041 | 0.075 | 0.213 |

研究期之前的训练段（2020-06..2021-06）15 列全部有限 99.76 %，所以块在训练窗口里没有「前半段为空」的时间标记。三条恒等式：`sjv / rv` 与 1 − 2 × `rv_down_share` 的最大偏差 4.9e-15；三段剖面复合与日线 `close / open − 1` 相差超过 1e-3 的股票日只有 0.01–0.02 %。**两条要带进第 0 轮的结论**：在沪深 300 上 `n_bars` 掩码不咬人，掩码实际只由 `rv` = 0 触发；`rku_l` 大半是价格水平（上表），它在增益里排第 3 这件事要先按这个读。

## 已知的起点：载体自己的账单

| 读数 | 值 | 出处 |
|---|---|---|
| 载体作必跑对照腿（100 万 / 沪深 300 成分 / 50 席） | 主动超额 **+4.30 %/年**、主动 IR **0.4642**、TE 9.26 %、β 0.851、3/4 年正 | `seq_gru_persist_1m_20260922` 的 `c_lgbm` |
| 同一分数另一条臂的第一轮 | +1.59 %/年、IR 0.18 | `xs_transformer_1m_20260922` 的 `c_lgbm` |
| 同一分数换到 10 万 / 12 席形状 | 主动 IR **0.0375 ≈ 0** | `lgbm_meta_100k_20260922` 的 `c_primary` |
| 同期零技能面板 | +2.20 %/年 | 同上第一条 |
| 冻结门 | 主动 IR ≥ 0.75、DSR ≥ 0.975（本轮默认） | 运行事实 |

**读法**：载体自己过不了冻结门。本臂不是要证明载体好，是要证明**这块列让它更好**，而且好得超过种子噪声（同族三个种子整窗读数散开约 **4 pp/年**）。

## 两条相邻的已关闭结论（先验，不是禁令，但都要在 `hypothesis` 里交代）

- **分钟 OFI 作独立排序分数**：`order_flow_ranker_20260917`（10 万 / 无），截面 IC 存活而组合尺度上没有多头腿。本块与它共享「文献效应偏空头腿」这一风险，G-INC2 的 Δ 就是冲着它设的。
- **彩票 / 反彩票作独立分数或 overlay**：`rmax_overlay_1m_20260921c`（100 万 / 授权，关的是授权形状）、`lottery_reverse_100k_20260921c`（10 万 / 无）。偏度与 RSJ 和最大日收益同族。

本臂两者都不是——它不把任何一列单独做成分数，而是把整块列加在一条已确认的载体上，并用同批的 `c_base` 定价。但**如果 `s_only` 赢而 `s1` 不赢**，本臂就滑向了前者，必须如实重写 `hypothesis`，并且**不得据此提名**。

## 诚实的限制

- **「日内分布矩在这条载体上有增量」没有实测支撑。** 文献最强的读数在美国、周频、小市值；本臂是中国、月度复核配 10 日标签、最大的 300 只。先验不高，已写进 `README.md` 与 `families.md`。
- **中国的证据本仓库只读过摘要。** 月频已实现偏度与 RSJ 的结论方向可以引用，数字不引。
- **剖面与成交量两组没有读过的出处。** 它们的登记符号是机制推断（等级 C）或不登记。
- **1 分钟采样的微观结构痕迹**：价差来回跳与 0.01 元最小价位会污染偏度与峰度；实测 `rku_l` 与对数收盘价的截面秩相关 0.59–0.64，它大半是价格水平列；RSJ 受影响最小。
- **收盘集合竞价**进了尾盘 30 分钟的各列与当天的矩；**涨跌停**截断了触板那一跳。这是 A 股数据的性质，不是错误，但美国证据里没有。
- **与载体的结构性重叠**：`intraday_stats` 与 Alpha158 读的是同一天的成交。RSJ 约三成与短期反转重叠，单日 `vwap_dev` 与 `VWAP0` 同义（因此不进块）。本块赌的是「路径形状」这一维不在那 158 个算子的值域里。
- **`intraday_stats` 是新表**：它的列定义以数据层的登记（`unit_reference.json`）为准，本包按交付时的登记写成；第 0 轮的三条恒等式就是为了在第一次读它时把定义上的偏差找出来。**覆盖终点跟随分钟按日层**，此后整段没有行：要回放到 Held-out 的臂必须自己确认终点。
- **`intraday` 的刷新不在夜间自动链里**，是一个 30 天回补窗口内的手工任务。对钉死发布的研究回放这无关紧要，对 Paper 的新鲜度有关。
- **月末成分截面**是股票池这一面最大的限制，两张之间的真实调整看不见（`references/index-membership.md`），供应商会回填历史权重，幅度未量化。
- **科创板与北交所整族被起步包从下单池里剔除**（一手粒度不同），训练仍然用它们；代价是逐年 1.7–4.8 pp 的指数权重。
- **10 日标签配月度复核是一处已登记的错配**，在本块上还叠着「跳跃效应月度消散」。它是本臂构造侧的主要风险，不是一个可以顺手改掉的常量。
- **本包不假设本臂的账户与授权。** 起步包按 100 万元不带授权写成（50 席）；运行事实不符时按 `families.md` 改登记。

## 外部出处

沙箱无网络，`references/` 下的六份笔记都是自写的，不含任何论文或受版权文本的转载。查证过的公开来源：

- Bollerslev, T., S. Z. Li, B. Zhao, "Good Volatility, Bad Volatility, and the Cross Section of Stock Returns", *Journal of Financial and Quantitative Analysis* 55(3), 2020, 751–781，doi:10.1017/S0022109019000097（作者公开版 <https://public.econ.duke.edu/~boller/Papers/JFQA_2020.pdf>）。本包引用的全部美国读数出自它的表 3、表 7 与正文；半方差的定义源自 Barndorff-Nielsen、Kinnebrock、Shephard（2010），已实现偏度与峰度的口径源自 Amaya、Christoffersen、Jacobs、Vasquez（经该文转引）。
- 「Does realized skewness predict the cross-section of Chinese stock returns?」，*Finance Research Letters*，2023（<https://www.sciencedirect.com/science/article/abs/pii/S1544612323007353>，只读过摘要）。
- A 股带符号跳跃风险的定价，*PLOS One*，2017（<https://journals.plos.org/plosone/article?id=10.1371%2Fjournal.pone.0181990>，只读过摘要）。
- 日内已实现波动的特质「波动的波动」与月度收益，*Finance Research Letters*，2025（只读过摘要）。
- 沪深 300 的编制与定期 / 临时调整规则：中证指数公司的指数编制方案与调整公告页（<https://www.csindex.com.cn>）。
- 信息比率 = 主动收益 ÷ 跟踪误差：Wikipedia 的 Information ratio 与 Tracking error 条目。
- Alpha158 是 Microsoft Qlib 的公开算子集合（<https://github.com/microsoft/qlib>）；本包的 `starter/lib/features.py` 是本仓库自己的向量化实现，逐字沿用同族包里的那一份。
