# 先并行只读，再单点实现

有效的开局是同一轮启动几个互斥的只读子代理（数据与单位、父策略与已有知识、参考材料），拿到结论后再交给一个可写子代理做计算或实现。

## 做法

- 只读块并行，写入块串行；同一文件同一时间只交给一个子代理。
- 一个子代理只做一件有界的事。把统计计算和实现改写拆成两个，不要塞进同一个长任务。
- task 里写清路径、约束和期望的返回格式；子代理汇报的是意图，先验收它实际写入的文件再依赖结论。
- task 会用到 shell 时带一行正确写法（`argv` 为字符串数组，如 `["python", "workspace/probe.py"]`）：子代理不继承你踩过的坑。
- 等待期间继续设计并启动下一块，不要轮询；结果会以消息送回。

## 教训

把计算和验证塞进同一个长子代理连续两折都拖慢了进度；也出现过汇报「已完成」但没有写入任何文件的交付，父会话只有验收文件才发现。一个折里 6 个子代理重复写错同一处 shell 调用格式 11 次。
